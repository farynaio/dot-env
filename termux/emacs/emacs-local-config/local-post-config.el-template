;; -*- mode: emacs-lisp; lexical-binding: t; -*-

;; Extra config file to run after init.el.
;; Use it to:
;; - setup workspaces: tabs, windows, layout etc.
;; - do some extra post init.el clean up
;; - and everything else that you want to run after init.el, but not want to share in version control repo

;; Copy it to local-post-config.el and edit as you wish

;; Example
;; (defun my/init ()
;;   ;; tasks file
;;   (tab-bar-new-tab)
;;   (find-file my/org-tasks-file-path)

;;   ;; elfeed
;;   (my/elfeed-start)

;;   ;; notmuch
;;   (tab-bar-new-tab)
;;   (notmuch)

;;   ;; journal
;;   (tab-bar-new-tab)
;;   (tab-bar-rename-tab "Journal")
;;   (my/org-journal-open-current-journal-file)

;;   ;; org-roam - placeholder
;;   (tab-bar-new-tab)
;;   (switch-to-buffer "*scratch*")

;;   ;; select first tab as start
;;   (tab-bar-select-tab 1))

;; (add-hook 'server-after-make-frame-hook #'my/init)

;; (unless my/emacs-initiated
;;   (my/init))